{
  "name": "mugznlugz-portal",
  "version": "1.0.0",
  "description": "Sleek black luxury portal for Mugznlugz — static site for GitHub Pages.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:check": "node build.js --check",
    "start": "node build.js && npx --yes serve .",
    "serve": "npx --yes serve ."
  },
  "engines": {
    "node": ">=14"
  }
}
